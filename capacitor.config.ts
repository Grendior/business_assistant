import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pl.witkac.kartamieszkanca',
  appName: 'Karta Mieszkańca',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
